image: microsoft/dotnet:latest

stages:
    - build
    - test

before_script:
    - "cd src/cappanna-helper-api"
    - "echo \"\" >| CappannaHelper.Api/Persistence/CappannaHelper.db"
    - "dotnet restore"

build:
    stage: build
    script:
        - "dotnet build"

test:
    stage: test
    script:
        - "dotnet test"